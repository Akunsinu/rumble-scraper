# =============================================================================
# Rumble Scraper Environment Configuration
# =============================================================================
# Copy this file to .env and modify as needed
# =============================================================================

# REQUIRED: Channels to backup (comma-separated)
# Examples:
#   CHANNELS=BonginoReport
#   CHANNELS=BonginoReport,TimcastIRL,RussellBrand
CHANNELS=

# Backup schedule (cron format)
# Default: Daily at 2:00 AM
# Examples:
#   0 2 * * *     = Daily at 2:00 AM
#   0 */6 * * *   = Every 6 hours
#   0 2 * * 0     = Weekly on Sunday at 2:00 AM
CRON_SCHEDULE=0 2 * * *

# Run backup immediately when container starts
RUN_ON_START=true

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Timezone
TZ=America/New_York

# User/Group IDs for file permissions
# Unraid default: PUID=99, PGID=100 (nobody:users)
PUID=99
PGID=100

# Path configuration (Unraid examples)
CONFIG_PATH=/mnt/user/appdata/rumble-scraper
DATA_PATH=/mnt/user/media/rumble_backups

# Web GUI port (default: 4000)
WEB_PORT=4000

# Optional: Limit videos per channel (leave empty for unlimited)
MAX_VIDEOS=

# Optional: Force re-download of all videos (use with caution)
FORCE_RESCAN=false

# Browser to extract cookies from (helps bypass Cloudflare)
# Options: chrome, firefox, edge, safari, opera
# Leave empty if not needed
BROWSER_COOKIES=

# Path to cookies.txt file (alternative to browser cookies)
# Leave empty if not using
COOKIES_FILE=
